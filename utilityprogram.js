// Storing user details
let userName = "Ganesh";
let age = 20;

// five subjects marks
let m1 = 85;
let m2 = 78;
let m3 = 90;
let m4 = 88;
let m5 = 92;

// Calculating total and average
let totalMarks = m1 + m2 + m3 + m4 + m5;
let averageMarks = totalMarks / 5

let upperCase = userName.toUpperCase()
let len = userName.length
//used template literal
console.log(`User Name : ${userName} , Age : ${age} , Uppercase Name  : ${upperCase} , Name Length : ${len} ,Total Marks : ${totalMarks} ,Average Marks   : ${averageMarks}`);